{
  "definitions": {
    "id": {
      "type": "string",
      "pattern": "\\w{24}"
    }
  },

  "type": "object",
  "properties": {
    "entity": {"enum": ["comment"]},
    "type": {"enum": ["wall", "video"]},
    "members": {"type": "array", "items": {"ref": "#/definitions/id"}}
  }
}
