{
  "definitions": {
    "id": {
      "type": "string",
      "pattern": "\\w{24}"
    }
  },

  "type": "object",
  "properties": {
    "title": {"type": "string"},
    "entity": {"enum": ["comment"]},
    "type": {"enum": ["wall", "video"]},
    "members": {"type": "array", "items": {"ref": "#/definitions/id"}}
  }
}
